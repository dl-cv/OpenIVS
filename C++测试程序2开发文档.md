# C++测试程序2开发文档

> 目标：定义 `dlcv_infer_cpp_qt_demo2` 的开发规格。程序业务与 `DlcvDemo2` 一致，工程形态与 `dlcv_infer_cpp_qt_demo3` 一致。开发与验收均以本文档为准。

## 1. 文档范围与定位

- 软件名称：`C++测试程序2`
- 项目目录：`dlcv_infer_cpp_qt_demo2`
- 工程文件：`dlcv_infer_cpp_qt_demo2/dlcv_infer_cpp_qt_demo2.vcxproj`
- 独立解决方案：`dlcv_infer_cpp_qt_demo2/build_only.sln`
- 技术栈：`C++17`、`Qt6 Widgets`、`OpenCV 4.10`、`nlohmann::json`、`dlcv_infer_cpp_dll`
- 启动入口：`dlcv_infer_cpp_qt_demo2/main.cpp`
- 核心界面与业务逻辑：`dlcv_infer_cpp_qt_demo2/MainWindow.h`、`dlcv_infer_cpp_qt_demo2/MainWindow.cpp`
- 图像显示控件：复用 `dlcv_infer_cpp_qt_demo/ImageViewerWidget.{h,cpp}`

本程序不是通用流程编辑器，也不是多模型路由平台。程序只围绕三个固定角色的模型工作：

- `元件提取模型`：整板滑窗检测，给出元件位置与带角度的类别名
- `元件检测模型`：对普通元件 ROI 做二级检测
- `IC 检测模型`：对 `IC`、`IC-BGA`、`IC-排阻`、`座子`、`开关`、`晶振` 的 ROI 做二级检测

最终界面只展示回写到整图坐标后的结果，不展示中间滑窗框和中间 ROI。

### 1.1 程序概述

程序具备以下主体能力：

- Qt 主界面、日志区、图像显示区、进度条、状态文本
- 分别选择元件提取模型、元件检测模型、IC 检测模型，并在选择完成后立即自动加载
- 选择单张图片后立即自动触发一次推理
- 记忆三个模型路径、图片路径和滑窗参数
- 一键串行加载三个模型，并输出单模型耗时和总耗时
- 命令行 `--load-three-models` 不启动窗体
- 释放当前已加载模型

### 1.2 关键交互要求

1. 用户浏览选择任一模型后，立即自动执行对应模型加载。
2. 用户浏览选择图片后，立即自动触发一次单次推理。
3. 主界面不显示设备选择、文档按钮、JSON 输出、一致性测试、速度测试。
4. 程序记忆三个模型路径、图片路径和滑窗参数，并在下次启动或再次打开文件对话框时恢复。
5. `执行推理` 按钮保留，用于对已选择图片重新执行推理。
6. `加载模型` 按钮保留，行为与自动加载一致。

## 2. 运行环境与依赖

- 操作系统：`Windows 10/11 x64`
- 编译环境：`Visual Studio 2022` + `v143`
- 编译平台：`x64`
- 解决方案级构建、项目级构建与发布前构建验证统一通过 MCP 构建工具执行，入口见 `开发文档.md` 的“统一编译说明”
- C++ 标准：`C++17`
- Qt 模块：`Qt6 Core`、`Qt6 Gui`、`Qt6 Widgets`
- OpenCV：`4.10`
- DLCV SDK 头文件目录：工程默认指向 `C:\dlcv\Lib\site-packages\dlcvpro_infer\include`
- OpenCV 头文件目录：工程默认指向 `C:\OpenCV\build\include`

### 2.1 工程链接关系

`dlcv_infer_cpp_qt_demo2.vcxproj` 的配置如下：

- 依赖工程：`..\dlcv_infer_cpp_dll\dlcv_infer_cpp_dll.vcxproj`
- 复用源码：`..\dlcv_infer_cpp_qt_demo\ImageViewerWidget.cpp`
- Debug 链接：`dlcv_infer_cpp_dll.lib`、`opencv_world4100d.lib`、`Qt6Widgetsd.lib`、`Qt6Guid.lib`、`Qt6Cored.lib`
- Release 链接：`dlcv_infer_cpp_dll.lib`、`opencv_world4100.lib`、`Qt6Widgets.lib`、`Qt6Gui.lib`、`Qt6Core.lib`

### 2.2 模型加载约束

- 三个模型都通过 `dlcv_infer::Model(path, 0)` 加载。
- `device_id` 固定为 `0`，界面没有 GPU/CPU 选择控件。
- `一键加载三个模型` 在后台线程中按元件提取模型、元件检测模型、IC 检测模型的顺序串行加载。
- 一键加载不加载单独的预热模型，也不执行额外推理。
- 命令行模式使用相同的加载与计时实现，不启动窗体。
- 现场机器若没有可用的 `GPU 0`，模型加载直接报错，文本区显示错误信息，不自动回退 CPU。

### 2.3 模型与图片文件类型

- 模型过滤器：`*.dvt`、`*.dvo`、`*.dvp`、`*.dvst`、`*.dvso`、`*.dvsp`
- 图片过滤器：`*.jpg`、`*.jpeg`、`*.png`、`*.bmp`、`*.gif`、`*.tiff`、`*.tif`

### 2.4 图像解码与送模规则

- 读取图片时使用 `cv::imread(path, cv::IMREAD_UNCHANGED)`，保留磁盘图像的原始通道数。
- 图像区显示使用解码后的原始 `Mat`，不在界面层额外改通道顺序。
- 送入 `Model` 前由本程序完成输入整理：
  - 灰度图：直接按单通道送入模型。
  - 三通道图：从 OpenCV 的 `BGR` 转为 `RGB` 后送入模型。
  - 四通道图：从 `BGRA` 转为 `RGB` 后送入模型。
- `loadCurrentImage` 同时保留 `bgrImage`（用于 UI 显示）与 `rgbImage`（用于推理）。
- `runPipeline` 及其内部滑窗、ROI 裁剪、旋转与二级检测都基于 `rgbImage` 执行。

## 3. 功能范围

### 3.1 必须具备的功能

- 分别选择并自动加载元件提取模型、元件检测模型、IC 检测模型，同时保留独立的 `加载模型` 按钮供手动重载
- 选择待推理图片后自动触发单次推理
- 设置滑窗参数：`窗口宽`、`窗口高`、`水平重叠`、`垂直重叠`
- 执行完整三模型推理
- 一键串行加载三个模型，并实时输出单模型耗时和总耗时
- 释放当前已加载模型
- 推理过程提供进度条显示
- 推理在后台线程执行，前端 UI 保持可响应
- 在文本区输出结果摘要
- 在图像区显示最终回写到整图坐标后的可视化结果
- 记忆最近一次三个模型路径、图片路径和滑窗参数

### 3.2 明确不提供的功能

- 不提供 GPU 选择控件
- 不提供 JSON 输出按钮和 JSON 文本显示
- 不提供一致性测试按钮
- 不提供批量文件夹推理
- 不提供流程编辑和规则配置页面
- 不提供 CPU 自动回退
- 不提供速度测试按钮及任何测速统计
- 不提供文档按钮

## 4. 主界面规格

### 4.1 主窗体

- 窗口标题：`C++测试程序2`
- 最小尺寸：`1200 x 900`
- 字体：`Microsoft YaHei`，`9pt`
- 使用 `QSettings("dlcv", "DlcvDemoQt2")` 保存窗口状态、三个模型路径、图片路径和滑窗参数
- 关闭窗口时若正在推理或加载模型，提示等待完成并忽略关闭；空闲时保存设置、等待线程退出并释放全部模型

### 4.2 界面布局

界面采用“顶部参数区 + 下方左右分栏”的结构：

```text
+------------------------------------------------------------------------------------------------------------------+
| 元件提取模型 [......................................] [浏览...] [加载模型]                                      |
| 元件检测模型 [......................................] [浏览...] [加载模型]                                      |
| IC检测模型   [......................................] [浏览...] [加载模型]                                      |
| 图片路径     [.............................................] [浏览...] [执行推理]                                |
| 窗口宽 [2560] 窗口高 [2560] 水平重叠 [1024] 垂直重叠 [1024] [释放模型] [一键加载三个模型]                      |
| 推理进度 [###############-----------------------] 42%  状态: 元件提取模型滑窗推理中                             |
|------------------------------------------------------------------------------------------------------------------|
| 结果文本区                                                                     | 图像可视化区                  |
| - 模型加载信息                                                                  | - 原图显示                    |
| - 推理结果摘要                                                                  | - 最终结果框/标签             |
| - 进度与状态                                                                    | - 鼠标缩放与拖拽              |
+------------------------------------------------------------------------------------------------------------------+
```

### 4.3 控件清单与默认值

- 文本框：`元件提取模型`
- 按钮：`浏览...`
- 按钮：`加载模型`
- 文本框：`元件检测模型`
- 按钮：`浏览...`
- 按钮：`加载模型`
- 文本框：`IC检测模型`
- 按钮：`浏览...`
- 按钮：`加载模型`
- 文本框：`图片路径`
- 按钮：`浏览...`
- 数值输入：`窗口宽`，默认 `2560`
- 数值输入：`窗口高`，默认 `2560`
- 数值输入：`水平重叠`，默认 `1024`
- 数值输入：`垂直重叠`，默认 `1024`
- 按钮：`执行推理`
- 按钮：`释放模型`
- 按钮：`一键加载三个模型`
- 进度条：`推理进度`（默认显示 `0%`，始终可见）
- 状态文本：`推理状态`
- 文本输出区：`QPlainTextEdit`
- 图像显示区：`ImageViewerWidget`

### 4.4 启动时状态恢复

程序启动后会自动恢复以下持久化项：

- `LastExtractModelPath`
- `LastComponentDetectModelPath`
- `LastIcDetectModelPath`
- `LastImagePath`
- `SlidingWindowWidth`
- `SlidingWindowHeight`
- `SlidingOverlapX`
- `SlidingOverlapY`

若设置文件中不存在这些值，则界面回落到默认值：

- `窗口宽 = 2560`
- `窗口高 = 2560`
- `水平重叠 = 1024`
- `垂直重叠 = 1024`

## 5. 交互流程

### 5.1 加载元件提取模型

- 点击 `元件提取模型` 右侧 `浏览...`，选择模型文件。
- 选择完成后立即实例化元件提取模型，并写入 `LastExtractModelPath`。
- 点击 `加载模型` 可在不重新选择文件的情况下再次实例化元件提取模型。
- 加载成功后，文本区输出加载成功信息和模型路径。
- 加载失败时，文本区显示异常信息。

### 5.2 加载元件检测模型

- 点击 `元件检测模型` 右侧 `浏览...`，选择模型文件。
- 选择完成后立即实例化元件检测模型，并写入 `LastComponentDetectModelPath`。
- 点击 `加载模型` 可在不重新选择文件的情况下再次实例化元件检测模型。
- 加载成功后，文本区输出加载成功信息和模型路径。
- 加载失败时，文本区显示异常信息。

### 5.3 加载 IC 检测模型

- 点击 `IC检测模型` 右侧 `浏览...`，选择模型文件。
- 选择完成后立即实例化 IC 检测模型，并写入 `LastIcDetectModelPath`。
- 点击 `加载模型` 可在不重新选择文件的情况下再次实例化 IC 检测模型。
- 加载成功后，文本区输出加载成功信息和模型路径。
- 加载失败时，文本区显示异常信息。

### 5.4 一键加载三个模型

- 点击 `一键加载三个模型` 后，程序先检查三个模型路径是否存在。
- 加载任务在后台线程执行，界面保持可响应。
- 开始计时前释放现有三个模型实例，释放耗时不计入总加载时间。
- 加载顺序固定为元件提取模型、元件检测模型、IC 检测模型，不并行加载。
- 每个模型开始加载时立即在文本区追加模型名称和路径。
- 每个模型构造完成后立即追加该模型加载耗时，单位为秒并保留两位小数。
- 三个模型全部加载后追加总耗时，单位为秒并保留两位小数。
- 一键加载过程不加载单独的预热模型，不执行额外推理或图片读取。
- 加载过程中禁止切换路径、推理、释放模型、再次加载或关闭窗体。
- 任一模型加载失败时停止后续加载，文本区追加失败信息，界面恢复可操作状态。

### 5.5 选择图片

- 点击 `图片路径` 右侧 `浏览...`，选择待推理图片。
- 图片路径写入 `LastImagePath`。
- 选择完成后自动触发一次推理，无需手动点击 `执行推理`。
- `执行推理` 按钮仍保留，支持用户在不重新选图的情况下再次推理。

### 5.6 命令行加载三个模型

命令格式：

```text
C++测试程序2.exe --load-three-models <元件提取模型> <元件检测模型> <IC检测模型>
```

- 三个模型路径必须同时提供，顺序固定。
- 程序不启动窗体，按元件提取模型、元件检测模型、IC 检测模型的顺序串行加载。
- 每个模型开始加载和加载完成时立即向标准输出写入信息。
- 单模型耗时和总耗时以秒为单位并保留两位小数。
- 命令行模式不加载单独的预热模型，不执行额外推理或图片读取。
- 参数错误或模型文件不存在时返回 `1`；加载异常时返回 `2`；全部完成时返回 `0`。

### 5.7 执行推理

- 推理触发方式有两种：选择图片后自动触发、点击 `执行推理` 手动触发。
- 触发后，程序先校验元件提取模型、元件检测模型、IC 检测模型和图片路径是否就绪。
- 任一项缺失时给出明确提示，不进入推理。
- 推理逻辑在后台线程执行，避免阻塞 UI 线程。
- 进度条始终显示：空闲状态为 `0%`，推理中按阶段递增，完成后显示 `100%`，下一次推理开始前重置为 `0%`。
- 推理过程中通过 UI 线程安全回调更新进度与状态文本，至少覆盖以下阶段：
  - 生成滑窗
  - 元件提取模型滑窗推理
  - 全图合并与 ROI 准备
  - 元件检测模型 / IC 检测模型局部推理与回写
  - 可视化刷新与文本汇总
- 推理进行中禁止重复进入同一推理流程。
- 推理成功后：
  - 图像区显示整图与最终检测结果。
  - 文本区输出本次推理摘要。
  - 当前图像保留在界面中，方便调参后再次执行。

### 5.8 释放模型

- 点击 `释放模型` 后，释放元件提取模型、元件检测模型和 IC 检测模型实例。
- 已记忆的模型路径、图片路径和滑窗参数不清空。
- 图像区保持当前显示内容，文本区输出释放完成信息。

## 6. 固定推理流程

### 6.1 第一步：整图滑窗与元件提取模型推理

- 按当前界面的滑窗参数对整板图像切片。
- 默认参数为：
  - `窗口宽 = 2560`
  - `窗口高 = 2560`
  - `水平重叠 = 1024`
  - `垂直重叠 = 1024`
- 对应步长为：
  - `step_x = 1536`
  - `step_y = 1536`
- 边缘窗口采用回退策略，保证最右侧和最下侧也被完整覆盖。
- 每个窗口都保留其在整图中的偏移量。
- 元件提取模型对每个窗口单独推理，得到窗口内元件结果。
- 每条结果在窗口阶段就被抬升到整图坐标系。
- 推理参数固定为 `with_mask = false`。

### 6.2 第二步：元件提取结果全图级合并

- 所有窗口结果进入统一的全图级去重合并。
- 合并粒度按完整 `category_name` 进行，不按基础器件名降维。
- `IC0` 与 `IC90`、`电阻0` 与 `电阻270` 不会被错误合并。
- 合并阈值固定为：
  - `iou_threshold = 0.2`
  - `combine_ios_threshold = 0.2`
- 代表框选择顺序：面积更大优先，面积相同则分数更高优先，再相同则 `order` 更小优先。

### 6.3 第三步：类别拆分与 ROI 裁剪

合并后的每个元件提取目标都会进入逐目标处理。

- 元件提取模型的 `category_name` 包含“基础类别 + 角度后缀”。
- 解析规则使用：
  - 正则：`^(.*?)(0|90|180|270)$`
- 解析成功时：
  - `base_name` 为前缀类别名
  - `angle` 为结尾角度
- 解析失败时：
  - `base_name = 原始 category_name`
  - `angle = 0`
- 典型示例：
  - `IC90 -> base_name=IC, angle=90`
  - `电阻270 -> base_name=电阻, angle=270`

ROI 裁剪规则如下：

- 元件提取模型若输出普通框，则按当前目标框的 `xywh` 从原图裁剪。
- 元件提取模型若输出旋转框或带角度信息，则按当前旋转框裁剪逻辑从原图获取 ROI。
- ROI 无效时跳过当前目标，并在文本区记录该目标被跳过。

### 6.4 第四步：ROI 逆时针归一到 0 度

- ROI 按解析出的角度逆时针旋转回 `0` 度视角。
- 例如元件提取模型类别为 `IC90`，则该 ROI 逆时针旋转 `90` 度后再进入后续检测。
- 再例如类别为 `电阻270`，则 ROI 逆时针旋转 `270` 度后进入后续检测。
- 角度归一仅作用于局部检测输入，不直接修改最终回写坐标。

### 6.5 第五步：按类别分流到对应检测模型

- 分流依据是拆分后的 `base_name`，不是原始带角度的 `category_name`。
- 当 `base_name` 为 `IC`、`IC-BGA`、`IC-排阻`、`座子`、`开关` 或 `晶振` 时，当前 ROI 路由到 `IC 检测模型`。
- 其余类别统一路由到 `元件检测模型`。
- 分流决策在每个 ROI 上独立进行，同一张整板图中可以同时走两条分支。

### 6.6 第六步：对应检测模型执行局部推理

- 被分配到的检测模型接收“按原目标框裁剪、已归一到 0 度”的 ROI。
- 元件检测模型与 IC 检测模型检测的主体都是“元件”本身，因此两者的主体输出类别统一为 `元件`。
- 二级检测模型若输出 `元件`，或为兼容老版本输出 `IC`，则将该检测框的最终 `category_name` 映射回当前 ROI 对应的元件提取模型 `category_name`。
- 若二级检测模型输出 `焊点`、`引脚`、`文字` 等其他细分类别，则保持该检测结果的 `category_name` 不变。
- 若当前 ROI 对应的检测模型无结果，保留该条元件提取结果，避免目标整体丢失。

### 6.7 第七步：局部结果回写到整图并统一合并展示

- 局部检测结果最初位于“旋转归一后的 ROI 坐标系”。
- 回写顺序固定为：
  1. 从归一坐标系逆旋转回 ROI 原始方向
  2. 再叠加 ROI 在整图中的偏移量
- 回写完成后，元件检测模型结果、IC 检测模型结果和必要的保留结果统一落在整图坐标系中。
- 所有来源的结果最终合并为单一整图结果列表。
- 图像区绘制和文本区输出都只使用整图坐标结果。

## 7. 结果组织与界面展示

### 7.1 最终结果结构

最终结果组织为单一整图结果列表，用于直接驱动 `ImageViewerWidget::setImageAndResults(...)`。

每条结果至少包含：

- `category_name`
- `score`
- `bbox` 或旋转框信息
- 整图坐标

### 7.2 图像区展示规则

- 图像区始终显示原图。
- 可视化对象只使用最终结果，不展示中间滑窗框和中间 ROI。
- 每条结果都绘制检测框和标签。
- 用户可以在图像区进行滚轮缩放、左键拖拽和右键复位。
- 图像区快捷键与 `C++测试程序` 保持一致：`V` 切换可视化叠加，`C` 在 `类别+分数`、`仅类别`、`不显示` 三种标签模式间循环，`+` / `=` 放大标签，`-` 缩小标签，`0` 重置标签倍率，`Ctrl + 滚轮` 只调整标签字体倍率。
- 无结果时仍显示原图，文本区输出“未检测到结果”。

### 7.3 文本区展示规则

文本区显示以下信息：

- 一键加载时三个模型各自的开始信息、模型路径和加载耗时
- 一键加载完成后的三个模型总耗时
- 当前图片路径
- 当前元件提取模型路径
- 当前元件检测模型路径
- 当前 IC 检测模型路径
- 当前滑窗参数
- 滑窗数量
- 元件提取模型合并后目标数
- 元件检测模型结果数
- IC 检测模型结果数
- 最终结果数
- 推理耗时
- 结果明细
- 错误与异常信息

一键加载文本示例：

```text
开始加载元件提取模型: Y:\测试模型\模型1-元件提取-20260721_120_50_s.dvst
元件提取模型加载完成，耗时 1.96 秒
开始加载元件检测模型: Y:\测试模型\模型2-元件检测-20260721_120_50_s.dvst
元件检测模型加载完成，耗时 4.23 秒
开始加载IC检测模型: Y:\测试模型\模型3-IC检测-20260721_120_50_s.dvst
IC检测模型加载完成，耗时 5.09 秒
三个模型加载完成，总耗时 11.28 秒
```

### 7.4 单次推理文本示例

```text
图片: D:\samples\board01.jpg
元件提取模型: D:\models\component_extract.dvt
元件检测模型: D:\models\component_detect.dvt
IC检测模型: D:\models\ic_detect.dvt
滑窗参数: 2560 x 2560, overlap=(1024, 1024)
滑窗数量: 6
元件提取模型合并后目标数: 3
元件检测模型结果数: 1
IC检测模型结果数: 2
最终结果数: 3
推理耗时: 68.37 ms

[1] IC90      score=0.99  rect=(1240.0, 860.0, 118.0, 46.0)
[2] IC-BGA0   score=0.95  rect=(1468.0, 802.0, 132.0, 128.0)
[3] 电阻270   score=0.97  rect=(1682.0, 942.0, 96.0, 54.0)
```

## 8. 关键业务规则示例

### 8.1 IC 分流示例

元件提取模型某条合并结果为：

- `category_name = IC90`

处理过程为：

- 解析得到 `base_name = IC`、`angle = 90`
- ROI 逆时针旋转 `90` 度，归一到 `0` 度视角
- 因为 `base_name = IC`，所以该 ROI 走 `IC 检测模型`

若 IC 检测模型输出：

- `元件`（或兼容老版本输出的 `IC`）

则将该检测框的类别映射回当前元件提取结果的 `category_name = IC90`，再回写到整图坐标，并进入最终结果列表。

### 8.1.1 晶振分流示例

元件提取模型某条合并结果为：

- `category_name = 晶振0`

处理过程为：

- 解析得到 `base_name = 晶振`、`angle = 0`
- ROI 无需旋转（或按 `0` 度归一）
- 因为 `base_name = 晶振`，所以该 ROI 走 `IC 检测模型`

若 IC 检测模型输出：

- `元件`（或兼容老版本输出的 `IC`）

则将该检测框的类别映射回当前元件提取结果的 `category_name = 晶振0`，再回写到整图坐标，并进入最终结果列表。

### 8.2 普通元件分流示例

元件提取模型某条合并结果为：

- `category_name = 电阻270`

处理过程为：

- 解析得到 `base_name = 电阻`、`angle = 270`
- ROI 逆时针旋转 `270` 度，归一到 `0` 度视角
- 因为 `base_name` 不属于 `IC`、`IC-BGA`、`IC-排阻`、`座子`、`开关`、`晶振`，所以该 ROI 走 `元件检测模型`

若元件检测模型输出：

- `元件`

则将该检测框的类别映射回当前元件提取结果的 `category_name = 电阻270`，再回写到整图坐标，并进入最终结果列表。

### 8.3 特殊条件规则示例

- 检测模型无结果：保留当前元件提取结果
- 元件提取类别角度解析失败：按 `0` 度处理
- ROI 裁剪超出图像范围：自动裁剪回有效区域
- 滑窗重叠区域出现重复目标：先映射到整图，再按全图规则合并为一条

## 9. 异常处理行为

- 角度解析失败：流程继续执行，当前目标按 `angle = 0` 处理
- ROI 裁剪越界：自动裁剪到图像有效区域，不中断流程
- ROI 无效：跳过当前目标并记录日志，不中断整图流程
- 检测模型无结果：保留对应的元件提取结果
- 单个 ROI 在局部推理时抛异常：记录异常并继续处理下一个目标
- 模型加载失败：文本区显示异常信息，界面保持可继续操作状态
- 一键加载任一模型失败：停止后续模型加载，保留此前已经成功加载的模型，文本区显示失败信息
- 一键加载进行中再次操作或关闭窗体：提示正在加载模型，不执行重复操作或关闭
- 图片解码失败：文本区显示失败信息，不崩溃
- 推理后台线程抛异常：异常回传主线程并显示在文本区，进度条与推理触发控件状态正确复位
- 推理进行中再次触发：提示当前正在执行推理，忽略重复请求
- 释放模型失败：文本区提示异常，但界面不中断

## 10. 代码文件与职责

项目至少包含以下关键文件：

- `dlcv_infer_cpp_qt_demo2/main.cpp`
- `dlcv_infer_cpp_qt_demo2/MainWindow.h`
- `dlcv_infer_cpp_qt_demo2/MainWindow.cpp`
- `dlcv_infer_cpp_qt_demo2/dlcv_infer_cpp_qt_demo2.vcxproj`
- `dlcv_infer_cpp_qt_demo2/app_icon.rc`
- `dlcv_infer_cpp_qt_demo/ImageViewerWidget.h`
- `dlcv_infer_cpp_qt_demo/ImageViewerWidget.cpp`

职责划分如下：

- `main.cpp`：无参时创建 `QApplication` 并启动主窗体；有参时执行 `--load-three-models`
- `MainWindow.h`：声明 UI 控件、三模型字段、滑窗配置、ROI 结构与推理入口
- `MainWindow.cpp`：实现界面构造、信号绑定、模型加载、单次推理、结果显示
- `ImageViewerWidget.*`：负责图像显示与结果可视化

## 11. 验收清单

- 项目名称为 `dlcv_infer_cpp_qt_demo2`
- 窗口标题为 `C++测试程序2`
- 元件提取模型路径、元件检测模型路径、IC 检测模型路径、图片路径支持记忆恢复
- 三组模型路径右侧均包含 `加载模型` 按钮
- 界面包含 `一键加载三个模型` 按钮
- 一键加载按元件提取模型、元件检测模型、IC 检测模型的固定顺序串行执行
- 一键加载不加载单独的预热模型，不执行额外推理或图片读取
- 文本区在每个模型开始和完成时实时追加信息，并显示单模型耗时
- 三个模型加载完成后文本区显示总耗时
- 一键加载在后台线程执行，加载期间相关控件禁用且窗体不能关闭
- 命令行传入三个模型路径后不启动窗体，实时输出三个单模型耗时和总耗时
- 选择三个模型文件后都会自动加载
- 点击对应 `加载模型` 按钮可再次实例化对应模型
- 选择图片后会自动触发推理，`执行推理` 按钮仍可手动触发
- 推理进度条默认显示 `0%` 且始终可见，推理阶段状态可见
- 推理在后台线程执行，窗口拖拽、最小化、切换时 UI 不明显卡死
- 模型加载固定使用 `device_id = 0`
- 界面中没有 GPU 选择、JSON 输出、一致性测试、测速入口和文档按钮
- 整图滑窗参数默认为 `2560/2560/1024/1024`
- 滑窗边缘覆盖完整，不漏右侧和下侧
- 元件提取模型结果先抬升整图坐标，再做全图级去重合并
- 合并按完整 `category_name` 生效，不混淆 `IC0` 与 `IC90`、`电阻0` 与 `电阻270`
- `IC`、`IC-BGA`、`IC-排阻`、`座子`、`开关`、`晶振` 会路由到 `IC 检测模型`，其他类别会路由到 `元件检测模型`
- 二级检测模型输出的主体类别统一为 `元件`，并映射回元件提取模型原始 `category_name`；为兼容老版本模型，输出 `IC` 时按与 `元件` 相同的规则映射；输出 `焊点` / `引脚` / `文字` 等其他类别时保持原结果不变
- 检测结果会先逆旋转回原方向，再回写到整图坐标
- 最终结果为所有分支结果的统一整图列表
- 文本区和图像区能互相印证结果正确性
