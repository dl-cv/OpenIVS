---
alwaysApply: true
---
我用的是 .Net Framework 4.7.2，所以不能用高级 .Net 版本。
如果是 .Net Framework，UI定义在Form1.Designer.cs写，代码逻辑在Form1.cs写。
如果是 WPF，要尽量用WPF的风格写，比如使用绑定显示。
我们写的代码要开源，所以要写得很规范，很有逻辑。
你需要使用工具编译，Debug，x64，确保我的工程能过编译。
【强制规则/最高优先级】任何“编译/构建/重建/发布前构建验证”操作，必须且只能调用 MCP 构建工具执行（例如 `build_project`、`build_solution`）。
严禁使用 `msbuild`、`dotnet`、`powershell` 或任何本地 shell 命令进行编译；即使用户未再次提醒，也必须持续遵守，不得以“临时验证/兜底”名义例外。
若 MCP 构建工具不可用、报错或权限不足：必须立即停止构建动作并先向用户报告，不允许私自切换到本地命令链路。
